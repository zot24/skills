# Implementation Plans

Generated by the improve skill on 2026-07-06 (audit at commit `7cb28b4`). Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row when
done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix sync-skill.sh failure reporting, dead code, MDX stripping | P1 | S | — | DONE — merged to main |
| 002 | Rebuild agent-skills as dual-spec authority (open spec + Anthropic docs) | P1 | M | 001 | DONE — merged to main |
| 003 | Fix command-file paths (${CLAUDE_PLUGIN_ROOT}) and CLAUDE.md inaccuracies | P2 | M | — | DONE — merged to main |
| 004 | Sync hygiene: x-engagement registration, CI freshness cache, dedup fix | P2 | M | — | DONE — merged to main |
| 005 | Repair 36 broken upstream sources + broken discovery scripts | P1 | L | — | DONE (reviewed; 10 commits `6d89d21..372f00d`, worktree agent-a87006380b8354fd3) |
| 006 | Re-source adguard docs from raw wiki markdown (~20k lines of HTML out) | P2 | M | — | DONE (reviewed; commit `2c9aa75`, worktree agent-aeb2ea263fca2ccf1) |
| 007 | Workflow injection hardening (upstream tag names, `${{ }}` splicing) | P1 | S | — | DONE (reviewed; commit `1441fd8`, worktree agent-adf002882eaf2df0d) |
| 008 | release-please owns version fields (drop inline bumps) | P2 | M | 009, 007 | DONE (reviewed; commit `55610af` atop cherry-picked 007+009, worktree agent-aff3436cf090b1a11) |
| 009 | Manifest consistency (skills.toml, release-please, README) + CI check | P1 | M | — | DONE (reviewed; commits `749a10a`+`3bf9011`, worktree agent-a9d5174ea6766b831) |
| 010 | claude-code-expert cleanup (cache, set -e, HTML conversion, stray doc) | P3 | S–M | — | DONE (reviewed; commit `1afcb79`, worktree agent-aef87818be51044a3) |
| 011 | Repurpose chat-sdk skill for the new Chat SDK (vercel/chat) | P1 | M | 005–010 merged | DONE (reviewed; commit `c640583`, merged as `6ebd81b` with chat-sdk conflicts resolved in 011's favor) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes (round 2, plans 005–010)

- **008 requires 009** (all 22 skills must be release-please-registered before inline bumps are removed) and should land after **007** (both edit the same two workflow files).
- **005, 006, 010** are independent of everything and of each other.
- Audited round 2 at commit `61449aa` (2026-07-06). Not audited this round: content quality of skills not named in findings; the x-engagement/safe-delete skill *content*; zskills itself (external repo).

## Dependency notes (round 1)

- **002 requires 001**: plan 002 re-syncs agent-skills docs from Mintlify `.md` endpoints that embed MDX `export` blocks; without 001's `extract_content` fix the corrupted-JSX problem would be re-imported into the new docs.
- **002 and 003 both touch `skills/agent-skills/commands/agent-skills.md`** on different lines (002: docs list; 003: path prefix). Either order works, but land them in separate commits/branches and rebase the second.
- **003 and 004 both touch `CLAUDE.md`** in the same skill-sources table region (003: portainerctl row; 004: x-engagement row). Same caution.

## Findings considered and rejected (round 2)

- **sync-docs `${{ }}` splicing as an exploitable injection** — downgraded to hardening (folded into plan 007): I traced the data flow; `changes`/`changed_skills`/`sync_reports` content is regex-constrained at construction (`$APP_[A-Z_]+` names, version numbers, URLs from the repo's own sync.json), so no upstream-controlled free text reaches the splice today.
- **sync-marketplace self-trigger loop** — not a bug: double-guarded (`[skip ci]` + GITHUB_TOKEN trigger suppression).
- **CHANGELOG stubs as stale relics** — they're normal release-please output; coverage gaps are the real issue (plan 009).
- **obsidian committed docs polluted** — false alarm: its committed docs are clean authored fallbacks; only the *sources* are broken (plan 005 removes them).
- **firecrawl nav artifacts / servarr orphaned docs / gh-issue-tracker 250-line SKILL.md** — real but low-value this round; deliberately not planned. gh-issue-tracker SKILL.md trim is the first candidate for a future docs pass.
- **broad discover-pages rollout (ai-sdk, immich, umami, glinet, obsidian, agent-browser)** — still deferred pending per-site llms.txt/sitemap probes; only chat-sdk (verified llms.txt, 6 dead URLs) made the cut into plan 005.

## Findings considered and rejected (round 1)

- **sync-report.txt committed accidentally** — false alarm: the root `.gitignore` ignores it globally. No action.
- **Registration drift across marketplace.json / README / CLAUDE.md / SKILLS array** — audited all 22 skills; fully consistent apart from the x-engagement case handled in plan 004. No action.
- **Version drift between sync.json / plugin.json / marketplace.json** — verified consistent for all 22 skills. No action.
- **Use an MCP server or the bundled Anthropic docs skill as the retrieval channel for agent-skills docs** (operator question) — rejected for sync purposes: this repo's architecture is committed cached docs updated by CI curl, and Anthropic's official machine-readable channel for that is exactly the `.md`/`llms.txt` endpoints plan 002 uses. Live-lookup tools (bundled `/claude-api` skill, docs MCP servers) complement but can't populate `docs/`.
- **Roll out discover-pages.sh to ai-sdk, immich, umami, chat-sdk, glinet, obsidian, agent-browser** — deferred direction item, not planned: needs a per-site check that each upstream actually serves `llms.txt`/sitemap before scripts are worth writing.
- **Repo-wide validation CI job (audit-checklist as CI)** — direction item offered but not selected for planning this round.

## New findings surfaced during execution (unplanned)

- **chat-sdk's upstream is gone** (found by plan 005): chat-sdk.dev has been repurposed for an unrelated chat-bot SDK product; one source had been silently syncing wrong-product content into `overview.md` for ~3 cycles. Plan 005 froze the docs and left only the vercel/ai-chatbot GitHub README as a source. OPERATOR DECISION NEEDED: repoint the skill at vercel/ai-chatbot's docs, or deprecate the skill.
- **hermes discovery will suggest `docs/skills/` as new** — it was deliberately dropped (JS-rendered duplicate of the tracked user-guide skills page); decline it or add a script exclusion.

- **servarr `discover-pages.sh` crawler is broken against the live site** (found by plan 004's executor): its regex expects wiki paths like `/en/sonarr/...` but wiki.servarr.com now emits `/sonarr/...`, so discovery always returns 0 pages. Out of scope for plan 004; candidate for a future plan.
- **Pre-existing broken upstream sources exposed by the CI dry run of 2026-07-07** (run 28831719688 — the fixed failure reporting made these visible for the first time): agent-browser (`agent-browser.dev/agent-mode`, HTTP 000), chat-sdk (6 `chat-sdk.dev/docs/*` URLs, HTTP 000 — site possibly moved), hermes (`/docs/ko/`, HTTP 000), firecrawl (4 URLs incl. `extract-get.md` and 3 MCP setup guides, HTTP 000), obsidian (6 URLs returning HTTP 200 but failing content validation — JS-rendered pages). Candidates for a future sync-source cleanup plan.
- **agentskills.io `clients.md` (Client Showcase) is deliberately untracked** (surfaced by plan 002's discover script): it's a logo/product showcase page with little instructional value. The new `agent-skills/discover-pages.sh` will keep reporting it as NEW; add it to sync.json or an ignore list if the noise bothers CI.

## Known follow-ups

- **MDX component tags in synced docs**: `extract_content` in sync-skill.sh strips a fixed Mintlify tag list but not Fumadocs components (`<Callout>`, `<TypeTable>`, `<PackageInstall>`, `<FeatureSupport>`); 66 of chat-sdk's 80 new docs carry residual tags (readable, but noise). Fix: extend the tag alternation + re-sync chat-sdk.

## Merge record (round 2)

Round-2 branches merged to local `main` on 2026-07-06 in order 005 → 006 → 010 → 008-branch (the last carries 007+009 as cherry-picks; their standalone branches were NOT merged to avoid duplicate patches). Zero conflicts. Post-merge verification: consistency checker green, all workflow YAML parses, no HTML in adguard, no version-bump code in workflows, clean tree.

## Merge record (round 1)

All four branches merged to local `main` on 2026-07-06, in order 001 → 002 → 003 → 004 (merge commits `f2dcd06`, `69545c6`, `4a07251`; 001 fast-forwarded). Two conflicts resolved as predicted: `skills/agent-skills/commands/agent-skills.md` (002's docs list + 003's path prefix — kept both) and `CLAUDE.md` (003's portainerctl row + 004's x-engagement row — kept both). Post-merge verification suite passed on main. Not pushed.
